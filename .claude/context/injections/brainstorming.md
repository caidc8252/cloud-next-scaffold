<!-- scaffold:injection:brainstorming -->
# Brainstorming — project context

- Specs come from upstream (FeiShu + `../pep-webapp-docs` + `../pep-data-model-docs`); most module work starts at planning from a populated spec, not a blank page — use this skill only when the spec itself is genuinely open.
- Keep scope to one module unit (`apps/web/modules/<cat>/<mod>/`); commons promotion is a separate step.
