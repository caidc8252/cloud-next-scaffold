---
name: coding
description: Use after /start-work, when ready to implement the started task's module.
disable-model-invocation: true
---

# /coding

- First run the `logic-analyze` skill — it distills the task's technical logic + gaps into `logic.md` (what the spec/prototype don't say but coding must know); plan from it.
- Then `superpowers:writing-plans` — the spec exists upstream, so don't brainstorm.
- Pipeline findings → `/fix`.
- Don't write back to FeiShu or open a PR here — that's `/submit-work`.
