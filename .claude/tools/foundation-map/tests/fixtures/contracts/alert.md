# Alert
## Implementations
- **Next / @cloud/ui** — `import { Alert, AlertTitle, AlertDescription } from "@cloud/ui"`.
- **Artifact (self-contained HTML)** — use `.alert` + `.alert--<tone>` with `.alert__title`. Reuse `.btn` (e.g. `.btn--ghost`) inside `.alert__action`.
